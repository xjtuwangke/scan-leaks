{
  "name": "scan-leaks",
  "version": "0.1.0",
  "description": "Standalone secret leak scanner with built-in regex and entropy detectors.",
  "main": "dist/index.js",
  "bin": {
    "scan-leaks": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/scan-case.test.mjs",
    "scan": "node dist/index.js",
    "start": "node dist/index.js"
  },
  "keywords": [
    "secret",
    "scan",
    "gitleaks",
    "security",
    "entropy",
    "rules"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^12.0.0",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.12.0",
    "typescript": "^5.4.5"
  }
}
